; Basalt Project Config — Per-game/template settings
; Edit this file to configure modules per project.
; Copy to your project root or use basalt-project.json for JSON format.

[project]
name=MyProject
template=fps

; Modules: true=enabled, false=disabled. Omit = use default from modules.config.js
[modules]
; lights=true
; dock-layout=true
; fps-controller=true
